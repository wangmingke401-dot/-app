# 黑马记账

一款本地桌面记账软件：记下每一笔人民币花销，按月查看账单，用图表看清钱花在了哪里。

## 目标平台

- **仅 Windows 10/11**（2026-08-21 用户拍板：不需要兼容 Mac 系统）
- 打包目标为 Windows 安装包（.exe）。

## 使用说明（如何安装与打开应用）

1. **正式安装**：双击 `dist/黑马记账-Setup-<版本>.exe`，一路「下一步」；安装完成后桌面会出现「黑马记账」图标，以后双击图标打开。
2. **首次打开警告**：Windows 可能弹出蓝色提示「Windows 已保护你的电脑」→ 点「更多信息」→「仍要运行」。原因：安装包没有购买微软数字签名（需付费），不是病毒。
3. **免安装试用**：双击 `dist/win-unpacked/黑马记账.exe` 直接打开，无需安装（无桌面图标）。
4. **数据备份**：所有账目保存在 `%APPDATA%/黑马记账/heima-accounting.db` 这一个文件里，复制它 = 备份全部账目；换电脑/重装前先备份此文件。

## 产品文档

### 产品定位

- 个人日常**支出**记账工具（第一版只记花销，不记收入）。
- 所有数据只存在用户自己的电脑上：**不联网、不上传**，隐私安全。
- 界面与项目内沟通全部使用中文。

### 第一版功能（MVP）

1. **记一笔账**：金额（人民币元，支持两位小数，必填）；分类（先选一级大类，再选二级小类）；日期（默认今天，可改）；备注（选填）。
2. **账单列表**：按月查看所有账目，按日期分组展示；每条显示：分类图标、分类名、备注、金额。
3. **编辑 / 删除**：点击账目可修改；删除时必须弹确认框，防止误删。
4. **统计图表**：
   - 月份切换（本月 / 上月 / 任意月）；
   - 该月总支出、总笔数、日均支出；
   - 一级分类占比环形图；
   - 各分类支出排行条形图；
   - 近 6 个月支出趋势折线图。

### 分类体系（二级，预设 10 大类 / 46 小类）

第一版分类为内置预设，不可自定义（"自定义分类"列入后续版本候选）。

| 一级大类 | 二级小类 |
|---|---|
| 🍚 餐饮 | 早餐、午餐、晚餐、零食饮料、水果、外卖、咖啡奶茶、聚餐 |
| 🚗 交通 | 公交地铁、出租网约车、火车高铁、飞机、加油充电、停车过路、共享单车 |
| 🛒 购物 | 日用品、服饰鞋包、数码家电、美妆个护、图书文具、宠物用品、其他购物 |
| 🏠 居住 | 房租、房贷、水电燃气、物业费、宽带话费、家具家电、维修装修 |
| 🎮 娱乐 | 电影演出、游戏充值、旅游度假、运动健身、会员订阅、KTV酒吧 |
| 🏥 医疗健康 | 门诊住院、药品、体检、保健用品 |
| 📚 教育学习 | 书籍课程、学费培训、考试报名 |
| 🧧 人情往来 | 请客送礼、红包礼金、孝敬长辈、爱心捐赠 |
| 💰 金融 | 手续费、利息、保险、投资亏损 |
| 📦 其他 | 其他 |

### 后续版本候选（暂不实现，待用户将来拍板）

导出账单 CSV、预算提醒、自定义分类、收入记录、多账本、云同步。

## 技术栈（决策记录，整个项目不再更换）

- **2026-08-21 用户拍板：Electron 方案**。当时对比了 3 个方案（Electron / Tauri / Flutter），选择 Electron 的理由：开发最稳定、生态最成熟、界面效果好；接受安装包较大（约 100MB）的缺点。
- **2026-08-21 用户拍板：仅 Windows，不需要 Mac 兼容**。

| 技术 | 作用（大白话） |
|---|---|
| Electron | 桌面外壳，让应用在 Windows / Mac 上运行（QQ、钉钉、VS Code 同款技术） |
| Vue 3 + Vite | 负责界面画面的框架和构建工具 |
| SQLite（better-sqlite3） | 账目数据库——电脑上的一个本地文件，无需安装数据库软件 |
| ECharts | 画统计图表 |
| electron-builder | 打包成 .exe（Windows）/ .dmg（Mac）安装包 |

## 数据与隐私

- 账目存放在本机 SQLite 文件（位于系统用户数据目录，如 Windows 的 `%APPDATA%/黑马记账/`），应用不发起任何网络请求。
- 金额一律以「分」（整数）存储，避免小数计算误差；展示时除以 100 并保留两位小数。
- 日期一律使用 `YYYY-MM-DD` 格式。

## ⚠️ 协作规则（最高优先级，整个项目期必须遵守）

1. **用户是技术小白**，无法提出技术性要求。任何技术相关决策，Claude 必须：列出 2~3 个方案 → 用通俗语言（生活化比喻）解释各自优缺点 → 给出明确推荐 → **由用户拍板决定**，不得自行定夺。
2. **决策流程**：重大决策（选技术栈、选功能方向等）用提问方式单独请用户确认；小决策随开发计划一并展示并解释，用户批准计划即视为同意。
3. **沟通方式**：全程中文；解释避免专业术语；每完成一步，用通俗语言告知用户做了什么、结果如何、如何验证。
4. 本规则持续有效，直到用户明确表示解除。

## 开发约定与常用命令

- 项目结构：`src/main`（后台：窗口 + 数据库）、`src/preload`（安全桥接）、`src/renderer`（界面，Vue 3）
- 数据文件：`%APPDATA%/黑马记账/heima-accounting.db`（SQLite；备份 = 复制此文件）
- 冒烟测试：`node scripts/smoke-test.mjs`（需先用调试端口启动应用，见脚本头部注释）

**本机环境注意事项**（用户在 Claude Code 沙箱中运行，与用户自己的终端不同）：

1. 用户终端里 `npm run dev` / `npm run package:win` 正常；在 Claude Code 的 Bash 沙箱中，npm 脚本因调用 cmd.exe 被拦截会失败，改用 `./node_modules/.bin/` 下的命令直接执行。
2. 沙箱 shell 会残留 `ELECTRON_RUN_AS_NODE=1` 环境变量，**启动应用前必须先 `unset ELECTRON_RUN_AS_NODE`**，否则应用以命令行模式运行而报错。
3. 打包必须用 **electron-builder ≥ 26**（旧版在本机因 Windows 符号链接权限失败）；Electron 与打包工具下载用 npmmirror 镜像加速：
   `ELECTRON_BUILDER_BINARIES_MIRROR=https://npmmirror.com/mirrors/electron-builder-binaries/ ELECTRON_MIRROR=https://npmmirror.com/mirrors/electron/ ./node_modules/.bin/electron-builder --win`
4. 安装依赖用 `npm install --ignore-scripts`；Electron 本体和 better-sqlite3 原生模块如缺失需手动下载解压（本机已装好，勿重复 `npm install` 触发脚本）。
5. 安装包输出在 `dist/黑马记账-Setup-<版本>.exe`。
